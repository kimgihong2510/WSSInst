/*
  ==============================================================================

    InstrumentalCondition.cpp
    Created: 5 Mar 2024 8:51:10pm
    Author:  JohnKim

  ==============================================================================
*/

#include "InstrumentalCondition.h"

/*
ICPad::ICPad() : mICPointVector()
{
    
}

void ICPad::mouseDown(const juce::MouseEvent& e)
{
    if (e.getNumberOfClicks() == 2)
    {
        auto newConditionPoint = mICPointVector.addConditionPoint();
        
        if(newConditionPoint == nullptr)
            return;
        
        (*newConditionPoint).setBounds(getLocalBounds().withSizeKeepingCentre(THUMBWIDTH, THUMBWIDTH));
        addAndMakeVisible(newConditionPoint);
    }
}
void ICPad::paint(juce::Graphics& g)
{
    auto bounds = getLocalBounds().toFloat();
    
    g.setGradientFill(juce::ColourGradient{ juce::Colours::black.brighter(0.2f), bounds.getTopLeft(), juce::Colours::black.brighter(0.1f), bounds.getBottomLeft(), false });
    g.fillRoundedRectangle(bounds, 10);
}

void ICPad::resized()
{
}
 */
